import Hero from '../../components/Hero';

const HomeSection = () => {
  return (
    <section id='home' className='min-h-screen w-full pt-11 md:pt-14'>
      <Hero />
    </section>
  );
};

export default HomeSection;
